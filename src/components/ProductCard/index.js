import React from "react"

const ProductCard = ({
    name, 
    image, 
    price, 
    discount,
    configuration,
}) => {
  return (
    <div>
        
    </div>
  )
}

export default ProductCard
